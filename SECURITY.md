# 安全说明

## 密钥管理

- 不要提交 `.env`、`/etc/routesnap-share.env`、服务器备份或已经填入真实 Token 的快捷指令。
- DeepSeek Key 和高德 Key 只应放在服务器端。
- `ROUTESNAP_ACCESS_TOKEN` 至少 32 个随机字符，建议使用 `openssl rand -hex 32`。
- 如果密钥曾进入 Git 历史，应在供应商控制台撤销并重新生成；仅删除最新提交中的字符串不够。
- 建议为高德 Web服务 Key 设置服务器出口 IP 白名单。

## 网络暴露

- 只向公网开放 80/443。
- 5001 仅监听回环地址或 Docker 内部网络。
- 必须使用 HTTPS，否则快捷指令 Token 和提交正文可能被窃听。
- `/` 和 `/health` 不需要鉴权，只暴露版本及“是否已配置”的布尔状态。

## 数据

服务器会处理你主动提交的分享文本，以及服务器抓取失败时 iPhone 取得的小红书页面正文。默认日志只记录长度、解析路径、命中数和耗时，不记录完整正文或 API Key。

SQLite 缓存可能包含地点名称、路线结果和小红书链接标识。备份与迁移数据卷时按私有数据处理。

## 访问 Token 的定位

当前 Token 是单用户/小范围分享的共享秘密，不是完整账号系统。若把同一个服务提供给大量不互信用户，应在反向代理层增加独立用户鉴权、速率限制、配额和审计，而不是给所有人同一个 Token。

## 漏洞报告

公开仓库启用 Issues 后，请不要在 Issue 中粘贴真实密钥、服务器 IP 白名单配置或完整私人帖子正文。涉及密钥泄露时先轮换密钥，再提交脱敏信息。
